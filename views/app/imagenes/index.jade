extend ../../layout.jade
block contenido
	div(class="container")
		h1 Mis Imágenes
		for imagen in imagenes
			.white.border.tr.large-padding
				.row
					.col-md-10
						h2=imagen.title
					.col-md-2
						a(href="/app/imagenes/#{imagen.id}/edit" class="right-space") Editar
						form(class="link" action="/app/imagenes/#{imagen.id}?_method=DELETE" method="POST")
							input(type="submit" value="Eliminar")